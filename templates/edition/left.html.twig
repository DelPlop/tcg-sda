{% trans_default_domain 'cards' %}
<h4 class="w3-bar-item"><b>{{ ('card.editions_list'|trans) }}</b></h4>
{% for edition in editions %}
    <a class="w3-bar-item w3-button w3-hover-black" href="{{ path('edition_cards', {edition: edition.id}) }}">{{ edition.name }}</a>
{% endfor %}
